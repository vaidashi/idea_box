
<h3>Category Listing for Idea Box</h3>

<ul>
  <% @categories.each do |category| %>
  <li>
    <%= category.name %> :
    <%= link_to "Edit", edit_admin_category_path(category) %>
    <%= link_to "Delete", admin_category_path(category), method: "delete" %>
  </li>
  <% end %>
</ul>
